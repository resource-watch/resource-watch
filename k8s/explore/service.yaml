apiVersion: v1
kind: Service
metadata:
  namespace: rw
  labels:
    name: resource-watch-explore
  name: resource-watch-explore
spec:
  type: NodePort
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 3000
  selector:
    name: resource-watch-explore
